const breakpoints = [575];
export const mq = breakpoints.map(
  bp => `@media (max-width: ${bp}px)`
);

export const colors = {
  primary: '#EB6652',
  secondary: '#B30500',
};

export const images = {
  logo: '/logo.png',
};
